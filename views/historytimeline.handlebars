<div>


    

    <script>
    $Lightning.use("c:DynamicSearch_LtngOut",    // name of the Lightning app
        function() {                  // Callback once framework and app loaded
            $Lightning.createComponent(
                "c:RecordTimeline", // top-level component of your app
                { 
                    "recordId": "a01j000000II79iAAD",
                    "objectApiName": "Interview__c",
                    "parentObjectIDFieldName": "Candidate__c",
                    "objectDateFieldName": "Interview_Date__c",
                    "objectDateDisplayFieldName": "Date__c",
                    "detail1Header": "Location:",
                    "detail1FieldAPIName": "Name",
                    "detail2Header": "Status:",
                    "detail2FieldAPIName": "Status__c",
                    "titleFieldAPIName": "Interview_Type__c"
                },                  // attributes to set on the component when created
                "lightningLocator",   // the DOM location to insert the component
                function(cmp) {
                    // callback when component is created and active on the page
                }
            );
        },
        'https://sparklelightningcommunity.force.com/'  // Community endpoint
    );
</script>


<div class="lcs-docpage-header">

        History Timeline

        

    </div>

<div id="lightningLocator"/>

</div>